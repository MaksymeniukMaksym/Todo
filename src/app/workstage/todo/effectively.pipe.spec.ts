import { EffectivelyPipe } from './effectively.pipe';

describe('EffectivelyPipe', () => {
  it('create an instance', () => {
    const pipe = new EffectivelyPipe();
    expect(pipe).toBeTruthy();
  });
});
